# В матрице элементы первого столбца возвести в куб.
from random import randint

# Размеры матрицы
rows = 3
cols = 3

matrix = [[randint(-3, 3) for i in range(cols)] for i in range(rows)]

print(matrix)